import { Component } from '@angular/core';

@Component({ //need to pass java script object to this decorator to configure this component 
    selector: 'app-header',// to use this component we need selector
    templateUrl:'./header.component.html'
})
export class HeaderComponent {

}